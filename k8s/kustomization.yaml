apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml
  - pvc.yaml
  - networkpolicy.yaml

commonLabels:
  app.kubernetes.io/name: unified-brain
  app.kubernetes.io/part-of: grobomo
