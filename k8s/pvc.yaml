apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: unified-brain-data
  labels:
    app: unified-brain
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
