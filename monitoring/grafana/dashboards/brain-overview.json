{
  "annotations": { "list": [] },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 1,
  "id": null,
  "links": [],
  "panels": [
    {
      "title": "Service Uptime",
      "type": "stat",
      "gridPos": { "h": 4, "w": 6, "x": 0, "y": 0 },
      "targets": [
        {
          "expr": "brain_uptime_seconds",
          "legendFormat": "uptime"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "unit": "s"
        },
        "overrides": []
      }
    },
    {
      "title": "Cycles Completed",
      "type": "stat",
      "gridPos": { "h": 4, "w": 6, "x": 6, "y": 0 },
      "targets": [
        {
          "expr": "brain_cycles_total",
          "legendFormat": "cycles"
        }
      ]
    },
    {
      "title": "Cycle Duration",
      "type": "gauge",
      "gridPos": { "h": 4, "w": 6, "x": 12, "y": 0 },
      "targets": [
        {
          "expr": "brain_cycle_duration_seconds",
          "legendFormat": "duration"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "unit": "s",
          "thresholds": {
            "steps": [
              { "color": "green", "value": null },
              { "color": "yellow", "value": 5 },
              { "color": "red", "value": 30 }
            ]
          }
        },
        "overrides": []
      }
    },
    {
      "title": "Events Ingested (by adapter)",
      "type": "timeseries",
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 4 },
      "targets": [
        {
          "expr": "brain_events_ingested_total",
          "legendFormat": "{{adapter}}"
        }
      ]
    },
    {
      "title": "Brain Decisions (by action)",
      "type": "piechart",
      "gridPos": { "h": 8, "w": 6, "x": 12, "y": 4 },
      "targets": [
        {
          "expr": "brain_decisions_total",
          "legendFormat": "{{action}}"
        }
      ]
    },
    {
      "title": "Events Processed (by source)",
      "type": "timeseries",
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 12 },
      "targets": [
        {
          "expr": "brain_events_processed_total",
          "legendFormat": "{{source}}"
        }
      ]
    },
    {
      "title": "Dispatch Results",
      "type": "timeseries",
      "gridPos": { "h": 8, "w": 6, "x": 12, "y": 12 },
      "targets": [
        {
          "expr": "brain_dispatch_results_total",
          "legendFormat": "{{outcome}}"
        }
      ]
    },
    {
      "title": "Respond Results",
      "type": "timeseries",
      "gridPos": { "h": 8, "w": 6, "x": 0, "y": 20 },
      "targets": [
        {
          "expr": "brain_respond_results_total",
          "legendFormat": "{{outcome}} ({{source}})"
        }
      ]
    },
    {
      "title": "Adapter Errors",
      "type": "timeseries",
      "gridPos": { "h": 8, "w": 6, "x": 6, "y": 20 },
      "targets": [
        {
          "expr": "brain_adapter_errors_total",
          "legendFormat": "{{adapter}}"
        }
      ]
    },
    {
      "title": "Processing Errors",
      "type": "stat",
      "gridPos": { "h": 4, "w": 6, "x": 12, "y": 20 },
      "targets": [
        {
          "expr": "brain_processing_errors_total",
          "legendFormat": "errors"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "thresholds": {
            "steps": [
              { "color": "green", "value": null },
              { "color": "red", "value": 1 }
            ]
          }
        },
        "overrides": []
      }
    }
  ],
  "schemaVersion": 39,
  "tags": ["unified-brain"],
  "templating": { "list": [] },
  "time": { "from": "now-1h", "to": "now" },
  "title": "Unified Brain Overview",
  "uid": "unified-brain-overview"
}
