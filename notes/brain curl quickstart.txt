● Working. Quick usage:

  ./scripts/brain-curl.sh health         # service status
  ./scripts/brain-curl.sh metrics        # Prometheus metrics
  ./scripts/brain-curl.sh stats          # webhook queue depth
  ./scripts/brain-curl.sh send '{"source":"test","title":"hello","body":"world"}'
  ./scripts/brain-curl.sh send-file event.json

  Ports default to 8790 (health) and 8791 (webhook), overridable via BRAIN_HEALTH_PORT /
  BRAIN_WEBHOOK_PORT env vars.

  Want me to continue with T046 (webhook rate limiting) and the rest of Phase 12, or is there
  something else you'd like to do with the brain service?
