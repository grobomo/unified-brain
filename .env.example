# Copy to .env and fill in your tokens
# cp .env.example .env

# GitHub personal access token (for gh CLI adapter)
GITHUB_TOKEN=ghp_your_token_here

# Anthropic API key (for API LLM backend in containers)
# Leave empty to use rule-based fallback
ANTHROPIC_API_KEY=
